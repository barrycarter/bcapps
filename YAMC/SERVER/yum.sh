# yum installs

yum -y install git bzip2 cpan gcc expect nodejs-ws golang yum-utils epel-release openssl openssl-devel certbot nmap-ncat nmap lsof ImageMagick nginx wget bind-utils transmission-cli docker perl-FCGI fcgi fcgiwrap

