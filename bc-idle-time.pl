#!/bin/perl

# determines idle time (X11 idle extension) and, for now, just prints
# it out; later, it will try to CONT some (all?) stopped processes
# when I idle for sufficiently long

require "/usr/local/lib/bclib.pl";

