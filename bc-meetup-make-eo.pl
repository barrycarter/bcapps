#!/bin/perl

# Given a download of your membership list from meetup.com, generates
# an iMarcos script that visits each users page and turns them into an
# event organizer

require "/usr/local/lib/bclib.pl";

while (<>) {
  debug("THUNK: $_");
}

