#!/bin/perl

# a simpler (but slower and more careful) version of
# bc-parse-rdfind.pl that subroutinizes out which files to consider
# and which file to link vs which file to keep

require "/usr/local/lib/bclib.pl";

