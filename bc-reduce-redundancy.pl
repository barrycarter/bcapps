#!/bin/perl

# Given a list of files, find redundant phrases in these files and
# replace them with tokenized shorter phrases

require "/usr/local/lib/bclib.pl";

# Step 1 (outside this script for now): cat/sort/uniq all files in
# question to "files.uniq"

