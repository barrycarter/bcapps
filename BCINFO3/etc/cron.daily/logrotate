#!/bin/sh

# dont have/use logrotate for now, so commenting out

# /usr/sbin/logrotate /etc/logrotate.conf
# EXITVALUE=$?
# if [ $EXITVALUE != 0 ]; then
#     /usr/bin/logger -t logrotate "ALERT exited abnormally with [$EXITVALUE]"
# fi
# exit 0

