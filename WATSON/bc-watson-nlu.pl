#!/bin/perl

# given one or more documents on the command line, run it through a
# default instance of Watson Natural Language Understanding (NLU)

# --username: username for NLU (may differ from other Watson credentials)
# --password: password for NLU (may differ from other Watson credentials)

require "/usr/local/lib/bclib.pl";





